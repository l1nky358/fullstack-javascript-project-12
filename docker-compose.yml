version: '3.8'

services:
  app:
    build: .
    ports:
      - "5001:5001"
    environment:
      - NODE_ENV=test
      - PORT=5001
    command: sh -c "node test-server.js & sleep 5 && npx playwright test"