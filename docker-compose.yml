version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    command: >
      sh -c "
        cd code &&
        make setup &&
        echo '=====================================' &&
        echo '✅✅✅ BUILD SUCCESSFUL! ✅✅✅' &&
        echo '=====================================' &&
        tail -f /dev/null
      "